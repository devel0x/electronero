# Blank App Template (V2-ready baseline)

Generated by `pydesk new <name> --template blank-app`.

## Included by default
- Permission-aware bridge methods (`backend/bridge.py`)
- Manifest-driven window/runtime config (`platform.toml`)
- Plugin loading from `plugins/*.py`

## Run

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ../platform_core
python desktop/main.py --dev
```
